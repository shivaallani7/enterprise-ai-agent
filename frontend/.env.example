# Set to true to skip MSAL login entirely (uses dev-token bypass on the backend).
# Requires APP_ENVIRONMENT=development in backend/.env.
VITE_AUTH_DISABLED=false

# Required when VITE_AUTH_DISABLED=false (production auth)
VITE_ENTRA_CLIENT_ID=your-client-id
VITE_ENTRA_TENANT_ID=your-tenant-id
